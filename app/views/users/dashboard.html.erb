
    <div class="image_goal">
    <%= image_tag(@user_goal.goal.image_goal) %>

    </div>
    <!--
      <div class="col-xs-6 col-sm-6">
        <div class="card">
          <div class="card-category">Balance
            <div class="card-description">
              <h2>$ <%= current_user.balance %></h2>
              <p></p>
            </div>
          </div>
        </div>
      </div> -->



    <div id="bar-2" class="bar-main-container emerald">
      <div class="goal_name">
         <span>Til your <%= @goal.name %></span>
      </div>
      <div class="wrap">
        <div class="bar-percentage" data-percentage= <%= (current_user.balance.to_f / @user_goal.amount_cents.to_f)* 100 %> ></div>
        <div class="bar-container">
          <div class="bar"></div>
        </div>
      </div>
    </div>

 <!--      <div class="col-xs-6 col-sm-6">
        <div class="card">
          <div class="card-category">the first goal</div>
            <div class="card-description">
              <h2>$ <%= @user_goal.amount_cents%></h2>
              <p></p>
            </div>
          </div>
        </div> -->

<div class="budget-title">

    <p>My Budgets</p>

</div>
<div class="row dash-budgets">
  <% @budgets.each do |budget| %>
  <% @cash_flows = CashFlow.where(budget_id: budget.id)%>
  <div class="button-listener">
    <%= render 'components/budget_cards', budget: budget %>
      <div class="dropdown-content hidden">
        <%= render 'budgets/show', budget: budget, cash_flows: @cash_flows %>
      </div>
    </div>
    <%end%>
</div>





